export enum CustomComponentName {
  //the values are persisted in kv, so they should not be changed without migration
  ScreenSummary = 'ScreenSummary',
  Screenshots = 'Screenshots',
  DesignBrief = 'DesignBrief',
  FlowChart = 'FlowChart'
}
